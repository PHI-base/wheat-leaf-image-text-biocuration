# Tested, mutually compatible versions (Python 3.9-3.11).
# NOTE: torch/torchvision wheels are platform/CUDA specific. For a GPU build,
# install from the official index, e.g.:
#   pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121
# numpy is held below 2.0 for compatibility with the torch/scikit-learn builds below.
torch==2.2.2
torchvision==0.17.2
transformers==4.40.2
pandas==2.2.2
openpyxl==3.1.2
numpy==1.26.4
scikit-learn==1.4.2
imbalanced-learn==0.12.2
matplotlib==3.8.4
Pillow==10.3.0
nltk==3.8.1

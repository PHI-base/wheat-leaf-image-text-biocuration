# Outputs folder

The training script writes model outputs here.

Expected outputs include:

- `cross_validation_results.json`
- `combined_confusion_matrix_bert_entity.png`
- `best_overall_model_bert_entity.pth`

Large model files should normally not be committed to GitHub unless there is a clear reason.
